import React from 'react';
import { Brain } from 'lucide-react';

const SecondBrainIcon = ({ size = 22, strokeWidth = 1.5 }) => {
  return <Brain size={size} strokeWidth={strokeWidth} />;
};

export default SecondBrainIcon;
