# ===== CORE FASTAPI =====
fastapi>=0.109.0
uvicorn>=0.27.0
pydantic>=2.6.3
pydantic-settings>=2.2.1
python-multipart>=0.0.6
python-dotenv>=1.0.0

# ===== AUTH & SECURITY (Member 1) =====
PyJWT>=2.12.0
bcrypt>=4.1.2
python-jose[cryptography]
passlib
authlib

# ===== SUPABASE & STORAGE (Member 1) =====
supabase>=2.28.0
boto3>=1.34.0
pypdf

# ===== DATABASE (shared) =====
sqlalchemy
psycopg2-binary
pgvector

# ===== UTILITIES =====
httpx>=0.26.0
email-validator>=2.0.0
anyio
typing_extensions
tzdata
tzlocal
slowapi

# ===== FILE MANAGER (Member 2 - Ashmitha) =====
PyMuPDF>=1.23.8
openai>=1.3.0
reportlab>=4.0.9
python-docx>=0.8.11
python-multipart

# ===== M3 STRUCTURED NOTES + M4 QUIZ (Members 3 & 4) =====
# These are imported at module load time -> missing them silently skips the
# /api/m3/* (notes) and /api/quizzes/* routes. Must stay in requirements.txt.
PyPDF2>=3.0.0
python-pptx>=0.6.23

# Cheap lazy-imported extractors so uploads of xlsx / ppt / epub / rtf / images work
openpyxl>=3.1.2
ebooklib>=0.18
beautifulsoup4>=4.12.3
striprtf
Pillow>=10.0.0
pytesseract>=0.3.13

# ===== TESTING =====
pytest>=7.4.4
pytest-asyncio>=0.23.0

# ==============m4========
# requirements — Quiz Module

# OpenRouter / OpenAI-compatible client
openai>=1.0.0

# Document extraction
PyPDF2
python-docx

# Spreadsheet extraction
openpyxl

# Presentation extraction
python-pptx

# Image OCR
pytesseract
Pillow

# Scanned PDF OCR (renders PDF pages as images for Tesseract)
PyMuPDF

# EPUB extraction
ebooklib
beautifulsoup4

# RTF extraction
striprtf

# PDF report generation
reportlab

# Rate limiting
slowapi

#=============AI==========
# Install ONLY if you work on AI/Notes features (Members 3 & 4)
# pip install -r requirements-ai.txt

# ===== AI / LANGCHAIN =====
langchain-openai
langchain-community
langchain-core
langchain-text-splitters
langchain-groq
langchain-huggingface
openai
tiktoken
sentence-transformers
chromadb

# ===== DOCUMENT PROCESSING (Member 3) =====
PyPDF2
python-pptx
python-docx
openpyxl
reportlab

# ===== SCHEDULING =====
APScheduler

# ===== MONGODB (if needed) =====
pymongo
